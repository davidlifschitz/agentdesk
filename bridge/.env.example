# Agent to use: claude-code | codex | aider
AGENT=claude-code

# Optional auth token — if set, all WS connections must send this as the first message
# AUTH_TOKEN=your-secret-token-here

# Port for the bridge server (serves both API and static UI)
PORT=7700

# Workspace root — defaults to current working directory
# WORKSPACE=/path/to/your/project
